# Frontend environment
# Leave unset in local dev — Vite proxy handles API routing automatically
# Set to your backend URL for production builds
VITE_API_URL=

# Dev server port (default: 3000)
VITE_PORT=