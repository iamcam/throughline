# Docker Compose — DB configuration
# These are used to construct DATABASE_URL for the backend container
# and to configure the db container itself
DB_NAME=podcast_engine
DB_USER=postgres
DB_PASSWORD=changeme